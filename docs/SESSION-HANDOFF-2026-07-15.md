# SESSION HANDOFF — 2026-07-14 후반 ~ 07-15

> 범위: **P2 메타필터 완성 → 스트레스 검증 → 실사용 오답(720) 해부 → 생성/가드레일 구조 재설계**.
> 브랜치: 전부 `main` 푸시·배포 (`0d8b6dc` → `83496ab`). 직전 핸드오프: `SESSION-HANDOFF-2026-07-14.md`.
> 테스트: api **1,549 passed** / frontend **75 passed**.

## 1. 엔티티 메타필터 (P2, 깔때기 1단계) — 완성 계보

`0d8b6dc` 기본 구현 → 실측 반복으로 4차 정밀화:
- **교집합 우선 결합**(`78623bf`): 매칭 별칭들의 ∩≠∅이면 교집합("A상품 약관"→1문서), ∅이면 합집합("A랑 B 비교"→양쪽). 분기 없이 집합 연산 하나로 두 케이스 해결.
- **브랜드 접두 제거판**(`55955da`): "간편암건강보험"(New 생략) 매칭. ASCII 접두 strip.
- **한정사 접미 '용' 제거판**(`3125fe5`): "나 유병력자인데"(구어) → (유병력자용) 문서 매칭. 실사고 720.
- 안전장치 불변: SAME_DOC 고정 스코프 불가침 / 무매칭=무필터 / 필터 빈손=무필터 폴백 / 과반 커버 별칭 자동 제외(회사명) / TTL 5분.
- 관측: `entity_filter_applied` 로그 + `trace_detail.filter.entity_filter`.

## 2. 스트레스 검증에서 나온 수정 (`bb6fd32`)

- **위임 타임아웃 120→240s**: 무답변 확장 재시도(1회 보증)가 서브 안에서 발동하면 (검색+생성)×2가 정당 — 120s가 그걸 잘라 복수 주제 질문 2위임 전건 타임아웃(degrade 메시지) 실사고.
- **비교 마커 "차이점|차이가"→"차이"**: "보장 내용 차이 간단히"가 STANDALONE으로 새던 갭.
- 검증된 조합: CROSS_DOC 분류 → 플래너 상품별 rag_search 분리 → **각 검색이 자기 교집합 필터** → 다양성 캡. 비교 질문의 이상적 형태.

## 3. 실사용 오답 해부 (요청 720) — 이 세션의 중심 사건

질문: "나 유병력자인데 입원... 자부담금 얼마?" (구어, 상품명 없음)
- **검색: 무죄 확정** — rerank audit로 1차 검색이 정답(공제 계산 조항)을 서로 다른 문서 3곳에서 채택했음을 입증.
- **생성(로컬 8B): 유죄** — ① 원문 "10만원과 30% **중 큰 금액**"(max)을 "**합산**"으로 재서술 + 자작 계산식, ② 건보 미적용자 특례(40%)를 조건절 없이 일반 기준처럼 제시. **숫자가 전부 원문에 있어 기존 faithfulness 숫자 검증 전부 통과** — 논리 결합 왜곡은 사각이었다.
- 대응 1(`70eda4b`): rag_instruction 수치 조항 규칙(원문 인용·연산 변경 금지·조건절 동반) — 완화책.
- 대응 2(`420e789`): `AIP_MAIN_LLM_BACKEND` — **최종 답변 LLM만** 상용 스왑하는 부분 스위치(판단 레이어·임베딩·리랭커는 로컬 유지, synthesize_llm=main이라 다중위임 종합도 커버). A/B 실측: Haiku로 21초/왜곡 0/조건절 정확 vs 8B 71~170초/왜곡. **현재는 크레딧 문제로 오버라이드에서 꺼둠(로컬)** — 켜는 법: `docker-compose.override.yml` api env에 `AIP_MAIN_LLM_BACKEND: anthropic` 한 줄.

## 4. 생성 품질 메커니즘 구조 재설계 (`8aebe4e`, `83496ab`)

사용자 구조 비판이 정확했다: "1000토큰 만들고 '확인 필요' 문구 하나로 파이프라인 재실행은 부적합 — **내용이 올바른지 검토하는 가드레일이 결정해야**".

최종 구조 (세 메커니즘 역할 분리):

| 상황 | 판정 | 처방 |
|---|---|---|
| 실질 무답변 (≤350자 또는 서두 100자 내 부재 선언) | `is_no_answer_dominant` (결정론) | 검색 2배 확장 재검색 (widen retry) |
| 내용 오류 (연산 왜곡 0.2 / deep_eval 근거실패 0.3) | **가드레일 score < 0.35** | 같은 컨텍스트 재생성 1회 (경고 주입, 재검색 없음) |
| 장문 답변 + 말미 관용구 | — | 보존 |

- **연산 왜곡 결정론 검출** (faithfulness quick-check 신규): 소스 "A와 B 중 큰 금액" ↔ 답변 같은 쌍 "합산" → 0.2. 정규식, LLM 없음.
- **스트림 경로 재생성**: 비스트리밍 그래프의 regenerate 노드 계약을 스트림에 이식. `trace(regenerate)` 관측, replace("") 후 재스트리밍.
- score 계층화: 0.2 왜곡 / 0.3 근거실패 / 0.5 경미(숫자·인용 워닝) / 1.0 통과.
- 스트림 조기 중단 아이디어는 **철회** — 문구 뒤 유용한 내용 여부를 알 수 없는 시점의 절단은 같은 결함의 악화판.

## 5. 관측·UX·기타

- **supervisor faithfulness_score 배선**(`3125fe5`): 서브 done → runner → SubAgentResult(신규 필드, hub 계약 갱신) → passthrough 응답 → 요청 로그·SSE done.
- **출처 클릭 404 + 어포던스**(`a017e0e`): 출처 칩을 문서 뷰어 링크로 승격(↗·밑줄), 본문 가짜 링크(http://문서:...)는 URL 검증으로 무해화. rehype-raw 도입(`<br>` 렌더)의 부작용이었음.
- **말풍선 진행 상태 문구**(`d469acd`): trace→사람 문장 매핑, 확장 재시도 시 사유 고지.
- **표기 규칙**(`52af093`): 가짜 링크 금지·한국어 유지·약관 수치 우선.
- **어드민 계정 추가**: wnstn1342@gmail.com (ADMIN/SECRET, bcrypt r10, 로그인 검증). 비밀번호 채팅 평문 노출 — 실서비스 전 변경 권장.
- **MLX 임베딩(8103) 행 실사고**: 프로세스 생존+응답 불능 → rag가 무한 대기. 복구 `launchctl kickstart -k gui/501/com.kms.mlx-embedding`. 파이프라인 로그가 entity_filter까지 나오고 침묵하면 임베딩 행 의심. **LinkMonitor에 MLX 서버 편입 후보.**
- insert_document external_id 정본 upsert(동시 반영분) 테스트 계약 갱신.

## 6. 다음 작업 (사용자 지정)

**시스템 전체 구조 정밀 진단 — 아키텍처 레벨 문제점 + 배선 전수 감사.**
이 세션에서 우연히 발견된 것들이 동기: latency_timer 순서 버그, supervisor 로그·score 미배선, 스트림 경로 volatile 유실, 재시도 메커니즘 중복 설계, MLX 행 시 무한 대기(서킷 부재 지점) 등 — "각 기능은 있는데 이어붙인 곳이 샌다"는 패턴. 레이어 경계(Gateway→Router→Agent→Tool 단방향), config/profile 필드→소비처 전수, SSE·done 페이로드 계약, 단일 장애점, 죽은 코드 관점으로 훑을 것.

추가 지정 축 2개:
1. **100명 동시 사용 가능 여부** — MLX 단일 GPU 서버 처리량, asyncio 블로킹, DB 풀, 타임아웃·백프레셔, 요청당 40~170초 점유의 동시성 함의.
2. **의미분석 동적 라우팅 취약성** — 패턴 매칭 + 8B 판단에 기대는 L0~L3·decompose·sticky의 실패 모드. 사용자 평가: "엄청 취약".
